# Flutter Orders

This folder contains a Flutter remake of the Python order management example.
It demonstrates how to load and store orders in an Excel file and display them
in a simple UI with three tabs (Pending, Ready, Delivered).

The original PyQt5 version allowed adding orders and updating their state.
This Flutter version keeps the same workflow.

Run with `flutter run` after adding Flutter to your path.
